<article><header class="page-hero"><div class="page-width"><div class="eyebrow">{{ article.tags.first | default: 'Research' }} / {{ article.published_at | date: '%d.%m.%y' }}</div><h1>{{ article.title }}</h1>{% if article.excerpt != blank %}<p>{{ article.excerpt }}</p>{% endif %}</div></header><section class="section"><div class="page-width"><div class="prose rte">{{ article.content }}</div></div></section></article>{% schema %}{"name":"Research article","settings":[]}{% endschema %}
