document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('[data-menu-toggle]'),nav=document.querySelector('[data-mobile-nav]');if(menu&&nav)menu.addEventListener('click',()=>{nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',nav.classList.contains('is-open'))});document.querySelectorAll('[data-cart-open]').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();document.querySelector('[data-cart-drawer]')?.classList.add('is-open')}));document.querySelectorAll('[data-cart-close]').forEach(b=>b.addEventListener('click',()=>document.querySelector('[data-cart-drawer]')?.classList.remove('is-open')));document.querySelectorAll('[data-track-metrics]').forEach(metrics=>{const value=metrics.querySelector('[data-track-value]'),period=metrics.querySelector('[data-track-period]');metrics.querySelectorAll('[data-track-range]').forEach(button=>button.addEventListener('click',()=>{metrics.querySelectorAll('[data-track-range]').forEach(item=>item.classList.remove('is-active'));button.classList.add('is-active');const range=button.dataset.trackRange;value.textContent=metrics.dataset[range];period.textContent=button.textContent}))});document.querySelectorAll('a').forEach(link=>{const label=(link.textContent||'').replace(/\s+/g,' ').trim().toUpperCase();if(/JOIN\s+(?:R&H|RH)\s+INTELLIGENCE/.test(label))link.href='/pages/r-h-intelligence';});});
